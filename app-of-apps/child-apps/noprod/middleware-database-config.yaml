apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: noprod-child-database-config
  namespace: openshift-gitops
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            env: noprod
  template:
    metadata:
      name: "{{name}}-database-config"
    spec:
      project: default
      source:
        repoURL: https://github.com/lfalero14/gitops-middleware-operator-config.git
        targetRevision: HEAD
        path: "./manifests/middleware-database/overlays/noprod"
      destination:
        server: "{{server}}"
        namespace: "database"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        #automated:
        #  prune: true
        #  selfHeal: true
        #  allowEmpty: false
