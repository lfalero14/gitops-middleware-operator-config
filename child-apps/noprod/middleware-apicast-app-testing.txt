apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: noprod-child-apicast-gateway-app-testing
  namespace: openshift-gitops
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            env: noprod
  template:
    metadata:
      name: "{{name}}-apicast-gateway-app-testing"
    spec:
      project: default
      source:
        repoURL: https://github.com/lfalero14/gitops-middleware-apicast-app.git
        targetRevision: develop
        path: "./overlays/noprod/testing"
      destination:
        server: "{{server}}"
        namespace: "apicast-gateway"
      #syncPolicy:
        #automated:
          #prune: true
          #selfHeal: true
